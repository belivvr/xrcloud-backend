export interface RoomOption {
    faviconUrl: string
    logoUrl: string
    returnUrl: string
    token?: string
    avatarUrl?: string
}

export interface RoomUrlData {
    userId?: string
    avatarUrl?: string
    linkPayload?: string
}

export enum SubscriptionStatus {
    active = 'active',
    paused = 'paused',
    canceled = 'canceled'
}

// export enum SubscriptionStatus {
//     active = 'active',
//     inactive = 'inactive'
// }
